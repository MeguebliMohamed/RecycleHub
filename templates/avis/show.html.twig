{% extends './FrontTemplate.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ avi.note }}</td>
            </tr>
            <tr>
                <th>Avi</th>
                <td>{{ avi.avi }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn btn-success" href="{{ path('app_avis_indexFront') }}">back to list</a>

    <a class="btn btn-info" href="{{ path('app_avis_edit', {'id': avi.id}) }}">edit</a>

    {{ include('avis/_delete_form.html.twig') }}
{% endblock %}
