<form method="post" action="{{ path('app_cadeaux_delete', {'id': cadeaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cadeaux.id) }}">
    <button class="btn">Delete</button>
</form>
