<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des réclamations</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
    <style>
        h1 {
            color: green;
            text-align: center;

        }
    </style>
</head>

<body>
<h1>Liste des réclamations Users</h1>
<table>
    <thead>
    <tr>
        <th>ID</th>
        {% for property, value in reclamations[0] %}
            <th>{{ property }}</th>
        {% endfor %}
    </tr>
    </thead>
    <body>
    <h2>Liste des réclamations</h2>
    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>Description</th>
            <th>Date de soumission</th>
        </tr>
        </thead>
        <tbody>
        {% for reclamation in reclamations %}
            <tr>
                <td>{{ reclamation.id }}</td>
                <td>{{ reclamation.description }}</td>
                <td>{{ reclamation.submissionDate ? reclamation.submissionDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </body>
</html>