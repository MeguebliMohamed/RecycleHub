<form method="post" action="{{ path('app_appelsoffres_delete', {'id': appelsoffre.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appelsoffre.id) }}">
    <button class="btn">Delete</button>
</form>
